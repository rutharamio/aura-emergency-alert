<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Aura" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Aplica el fondo degradado a todas las pantallas -->
        <item name="android:windowBackground">@drawable/bg_welcome</item>

        <!-- Fuente global (opcional si ya configuraste Poppins) -->
        <item name="android:fontFamily">@font/font</item>
    </style>

    <style name="Theme.Aura" parent="Base.Theme.Aura" />
</resources>
